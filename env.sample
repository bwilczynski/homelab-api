# Path to the YAML configuration file (default: config.yaml)
# CONFIG_FILE=config.yaml

# Backend credentials referenced from config.yaml via ${VAR} syntax.
# Add variables here matching your config.yaml references.
